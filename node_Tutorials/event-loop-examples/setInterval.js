// setInterval will keep executing untill and unless it is stopped.

// In this example it will run every 2 seconds

// setInterval is asynchronous
setInterval(() => {
  console.log("hello World");
}, 2000);
console.log("I will run First");
